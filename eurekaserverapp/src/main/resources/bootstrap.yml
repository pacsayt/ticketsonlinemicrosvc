#server:
#  port:8888

spring:
  application:
    name:eurekaserverapp
  profiles:
    active: composite
  cloud:
    config:
      server:
        git:
          uri: /home/tamas/projektek/TicketsOnline/ticketsonlinecfg
          cloneOnStart: true

management:
  security:
    enabled:false
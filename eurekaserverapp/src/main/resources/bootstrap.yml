server:
  port:8888

spring:
  application:
    name:eurekaserverapp
  profiles:
    active: composite
  cloud:
#    config:
#      server:
#        git:
#          uri: file:/${user.home}/projektek/TicketsOnline/ticketsonlinecfg
##          cloneOnStart: true
#          skipSslValidation: true
    loadbalancer:
      ribbon:
        enabled:false

management:
  security:
    enabled:false